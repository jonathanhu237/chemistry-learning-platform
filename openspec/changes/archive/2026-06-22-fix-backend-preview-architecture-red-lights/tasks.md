## 1. Backend Architecture Red Lights

- [x] 1.1 Remove the catalog preview domain's import of web/runtime auth types while preserving preview token claims.
- [x] 1.2 Add the currently registered preview, thumbnail-stream, and search diagnostics routes to `backend_route_inventory.json`.

## 2. Verification

- [x] 2.1 Run OpenSpec validation plus backend architecture, admin route, and catalog preview tests.
