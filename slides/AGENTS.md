# AGENTS.md

## Instructions

- When creating or suggesting Git commits, use Conventional Commits by default.
- When running Python code, you may use mamba to create or activate an existing virtual environment and run the Python code inside that environment.

## Project Notes

- 这个项目主要以 HTML 的方式制作 slides。
- 应用展示截图优先使用本地运行的 `~/code/chemistry-admin`，不要再从 `222.200` 远端环境截图。
- 本地截图优先使用旧版演示端：
  - 学生端：`http://127.0.0.1:15176/`（Vite 调试时用 `apps/web-student-old`，端口冲突则换下一个空闲端口）
  - 教师端：`http://127.0.0.1:15177/`（Vite 调试时用 `apps/web-teacher-old`，端口冲突则换下一个空闲端口）
- 远端地址仅用于必要时对照线上行为，不作为 PPT 截图来源：
  - 学生端：http://222.200.161.222/
  - 教师端：http://222.200.161.222:8080/
- 代码位置：`~/code/chemistry-admin`
- 可供参考的 PPT 位于 `references/` 目录。
- 制作或修改 slides 时，应使用 `ui-ux-pro-max`（UI-UX-Pro）skill 来指导页面设计与实现。
- 每次生成或修改 slide 后，需要自行预览检查格式，确认没有文字遮挡、溢出、错位或排版混乱。
